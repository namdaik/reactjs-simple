import axios from 'axios';
import * as Config from './../const';

import callApi from './apiCaller';


export default function partHtml(path){
    callApi(path).then(res => {
    	
    });
};